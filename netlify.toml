[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  timeout = 26

# Redirect all routes to index.html so React handles navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
